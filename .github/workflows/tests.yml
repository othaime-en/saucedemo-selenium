name: 🧪 SauceDemo Test Suite

# Trigger conditions
on:
  push:
    branches: [main, develop] # Run on pushes to main and develop branches
  pull_request:
    branches: [main] # Run on PRs to main branch
  schedule:
    - cron: "0 2 * * *" # Run daily at 2 AM UTC (smoke tests)
  workflow_dispatch: # Allow manual triggering

# Environment variables available to all jobs
env:
  NODE_VERSION: "18"
  HEADLESS: true
  CI: true

jobs:
  # Job 1: Run tests on different Node.js versions (compatibility testing)
  test-matrix:
    name: 🧪 Test on Node ${{ matrix.node-version }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16] # Test on multiple Node.js versions

    steps:
      # Step 1: Checkout the code
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      # Step 2: Setup Node.js environment
      - name: 🚀 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      # Step 3: Install dependencies
      - name: 📥 Install Dependencies
        run: |
          npm ci
          npm ls > /dev/null 2>&1

      # Step 4: Setup Chrome browser for testing
      - name: 🌐 Setup Chrome Browser
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      # Step 5: Run linting (code quality check)
      - name: 🔍 Run Code Linting
        run: npm run lint > /dev/null 2>&1
        continue-on-error: true # Don't fail the build on linting errors

      # Step 6: Run the test suite
      - name: 🧪 Run Test Suite
        run: |
          echo "Running tests with Node.js ${{ matrix.node-version }}"
          npm run test:e2e > /dev/null 2>&1
        env:
          BROWSER: chrome
          HEADLESS: true

      # Step 7: Upload test artifacts (screenshots, reports)
      - name: 📸 Upload Test Artifacts
        uses: actions/upload-artifact@v4
        if: always() # Always upload, even if tests fail
        with:
          name: test-artifacts-node-${{ matrix.node-version }}
          path: |
            reports/
            !reports/*.tmp
          retention-days: 7

      # Step 8: Upload test results
      - name: 📊 Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-node-${{ matrix.node-version }}
          path: test-results.xml
          retention-days: 30

  # Job 2: Cross-browser testing (Chrome and Firefox)
  cross-browser-test:
    name: 🌐 Cross-Browser Testing
    runs-on: ubuntu-latest

    strategy:
      matrix:
        browser: [chrome, firefox]

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🚀 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: 📥 Install Dependencies
        run: npm ci

      # Setup different browsers based on matrix
      - name: 🌐 Setup Chrome
        if: matrix.browser == 'chrome'
        uses: browser-actions/setup-chrome@latest

      - name: 🦊 Setup Firefox
        if: matrix.browser == 'firefox'
        uses: browser-actions/setup-geckodriver@latest

      - name: 🧪 Run Cross-Browser Tests
        run: |
          echo "Running tests on ${{ matrix.browser }}"
          npm test
        env:
          BROWSER: ${{ matrix.browser }}
          HEADLESS: true

      - name: 📸 Upload Browser-Specific Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cross-browser-artifacts-${{ matrix.browser }}
          path: reports/
          retention-days: 7

  # Job 3: Performance testing (measure test execution time)
  performance-test:
    name: ⚡ Performance Testing
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🚀 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: 📥 Install Dependencies
        run: npm ci

      - name: 🌐 Setup Chrome
        uses: browser-actions/setup-chrome@latest

      - name: ⚡ Run Performance Tests
        run: |
          echo "Running performance-focused tests"
          time npm test  # Measure execution time
          echo "Test execution completed"
        env:
          BROWSER: chrome
          HEADLESS: true
          TEST_TYPE: performance

      - name: 📊 Generate Performance Report
        run: |
          echo "## Performance Test Results" >> performance-report.md
          echo "- Test Suite Execution Time: $(date)" >> performance-report.md
          echo "- Browser: Chrome (Headless)" >> performance-report.md
          echo "- Node.js Version: 18" >> performance-report.md

      - name: 📈 Upload Performance Report
        uses: actions/upload-artifact@v4
        with:
          name: performance-report
          path: performance-report.md
